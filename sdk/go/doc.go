// Package sdk will hold the Go plugin SDK (SDK-100 ff.): the out-of-process
// gRPC plugin protocol client/server helpers and node config schema types.
// Empty placeholder until Increment 6+ needs a real plugin protocol.
package sdk
